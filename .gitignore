*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
.kotlin
#So we don't accidentally commit our private keys
*.gpg

# JS/Wasm coverage toolchain (c8 installed via npm)
node_modules/

# Isolated Gradle home inside the worktree (dev-flow convention)
.gradle-home
