
/* updated version of dep, introducing many uncompatable changes */

// class UserData(val name: String, val age: Int)

// fun greetUser(name: String): String {
//     return "Hello, $name!"
// }

fun calculateSum(a: Int, b: Int, c: Int): Int {
    return a + b + c
}

// fun processUserData(data: UserData): String {
//     return "User: ${data.name}, Age: ${data.age}"
// }

object ConfigHelper {
    const val VERSION = "1.0.0"
    
    // fun getConfigValue(): String {
    //     return "config-value"
    // }
}
