package com.example.dep

/**
 * INCOMPATIBLE version of the dependency library.
 * This demonstrates various binary incompatibility scenarios that partial linkage helps detect.
 * 
 * Changes from the original:
 * 1. greetUser() function is REMOVED
 * 2. calculateSum() signature changed (added third parameter)
 * 3. UserData class is REMOVED
 * 4. processUserData() is REMOVED (depends on UserData)
 * 5. ConfigHelper.getConfigValue() is REMOVED
 */

// greetUser() function REMOVED - caller-lib and ios-app use this!

// calculateSum() signature changed - now requires 3 parameters instead of 2
fun calculateSum(a: Int, b: Int, c: Int): Int {
    return a + b + c
}

// UserData class REMOVED - caller-lib uses this!
// class UserData(val name: String, val age: Int)

// processUserData() REMOVED - caller-lib uses this!
// fun processUserData(data: UserData): String

object ConfigHelper {
    const val VERSION = "2.0.0-INCOMPATIBLE"
    
    // getConfigValue() REMOVED - caller-lib uses this!
    // fun getConfigValue(): String
}
